// This file is part of the materials accompanying the book
// "The Elements of Computing Systems" by Nisan and Schocken,
// MIT Press. Book site: www.idc.ac.il/tecs
// File name: projects/10/Square/Main.jack

/**
 * The Main class initializes a new Square Dance game and starts it.
 */
class Main {
  static  int poo, foo;
  field char ch1;
field boolean boo;

    /** Initializes a new game and starts it. */
    function void main(int dummy, boolean bummy) {
        var SquareGame game;
var SquareGame silly, billy;
let pooface[0] = false;
        let game = SquareGame.new(3,4+5);
        if (3 > 0) {
          let game=null;
        }
        else { do x.y(~silly, billy, game[i], 3 + (5+7)); }
        while (3 < 5) {
          do game.run();
          do game.poo();
        }

  do game.dispose();

        return this;
    }
}
